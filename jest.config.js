const config = {
    transform: {},
    testEnvironment: 'node',
    moduleFileExtensions: ['js', 'mjs'],
    testPathIgnorePatterns: [
        "/node_modules/",
        "/kc-app/",
        "/client/"
    ],
};

export default config;
