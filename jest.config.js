const config = {
    transform: {},
    testEnvironment: 'node',
    moduleFileExtensions: ['js', 'mjs'],
    testPathIgnorePatterns: [
        "/node_modules/",
        "/kc-app/",
        "/keymaster-app/"
    ],
};

export default config;
